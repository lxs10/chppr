set.seed(0)

# consumer key and secret
paste(sample(sort(c(LETTERS, letters, 0:9)), 22, TRUE), collapse = "")
paste(sample(sort(c(LETTERS, letters, 0:9)), 43, TRUE), collapse = "")

# token and token secret
paste(sample(sort(c(LETTERS, letters, 0:9)), 16, TRUE), collapse = "")
paste(sample(sort(c(LETTERS, letters, 0:9)), 16, TRUE), collapse = "")
